import StartupLayout from '@/components/layout/StartupLayout';

export default function StartupRouteLayout({ children }: { children: React.ReactNode }) {
  return <StartupLayout>{children}</StartupLayout>;
}
